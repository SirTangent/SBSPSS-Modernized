/*	Shadow of the PSY-Q memory.h for the Win32 port (same reason as
	port/include/strings.h: the vintage header declares memcpy/memset/memcmp
	with empty parameter lists).  The PS1 build never sees this file.
*/
#ifndef _PORT_SHADOW_MEMORY_H
#define _PORT_SHADOW_MEMORY_H

#include <string.h>

#endif
